Deploying to multiple zones:

Foreach step:
    1 deployment per zone

Storage Adapter:
    1 DaemonSet per zone.



Deploying to Kubernetes in the cloud.

mkdir -p /store/perm_storage
mkdir -p /store/inputs
mkdir -p /store/outputs

Joining a cluster
Creating a virtual network spanning multiple zones in Azure.

Tagging the node with their correct zones.